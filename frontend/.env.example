# Copy to frontend/.env.local to override. Default "/api" is proxied to the backend by Vite in dev.
# For a production build point this at the backend origin, e.g. http://127.0.0.1:8000
VITE_API_BASE_URL=/api
